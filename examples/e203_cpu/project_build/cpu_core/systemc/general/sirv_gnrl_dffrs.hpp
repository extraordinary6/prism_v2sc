// Generated by prism_v2sc
#pragma once

#include <array>
#include <systemc>
#include <string>
#include <type_traits>

using namespace sc_core;
using namespace sc_dt;

#ifndef PRISM_V2SC_INTEGER_TYPES
#define PRISM_V2SC_INTEGER_TYPES
template <int Width> using prism_v2sc_uint_t = typename std::conditional<(Width <= 64), sc_uint<Width>, sc_biguint<Width>>::type;
template <int Width> using prism_v2sc_int_t = typename std::conditional<(Width <= 64), sc_int<Width>, sc_bigint<Width>>::type;
constexpr int prism_v2sc_clog2(int value) { int result = 0; for (int current = value - 1; current > 0; current >>= 1) ++result; return result; }
#endif

#ifndef PRISM_V2SC_MOD_SIRV_GNRL_DFFRS_HPP
#define PRISM_V2SC_MOD_SIRV_GNRL_DFFRS_HPP

template <int DW = 1>
SC_MODULE(sirv_gnrl_dffrs) {
  sc_in<prism_v2sc_uint_t<(((DW-1) >= (0)) ? ((DW-1) - (0) + 1) : ((0) - (DW-1) + 1))>> dnxt;
  sc_out<prism_v2sc_uint_t<(((DW-1) >= (0)) ? ((DW-1) - (0) + 1) : ((0) - (DW-1) + 1))>> qout;
  sc_in<bool> clk;
  sc_in<bool> rst_n;

  sc_signal<prism_v2sc_uint_t<(((DW-1) >= (0)) ? ((DW-1) - (0) + 1) : ((0) - (DW-1) + 1))>> qout_r;

  void assign_0() {
    qout.write(qout_r.read());
  }

  void always_ff_0() {
    auto __next_qout_r = qout_r.read();
    if ((rst_n.read() == 0b0)) {
      __next_qout_r = sc_uint<1>((sc_uint<1>(0b1)));
    } else {
      __next_qout_r = dnxt.read();
    }
    qout_r.write(__next_qout_r);
  }

  SC_CTOR(sirv_gnrl_dffrs) {
    SC_METHOD(assign_0);
    sensitive << qout_r;

    SC_METHOD(always_ff_0);
    sensitive << clk.pos() << rst_n.neg();

  }
};

#endif  // PRISM_V2SC_MOD_SIRV_GNRL_DFFRS_HPP
