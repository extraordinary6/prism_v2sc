// Generated by prism_v2sc
#pragma once

#include <array>
#include <systemc>
#include <string>
#include <type_traits>

#include "../tech/e603_gnrl_tech_ram__prism_p_88c924c340.hpp"

using namespace sc_core;
using namespace sc_dt;

#ifndef PRISM_V2SC_INTEGER_TYPES
#define PRISM_V2SC_INTEGER_TYPES
template <int Width> using prism_v2sc_uint_t = typename std::conditional<(Width <= 64), sc_uint<Width>, sc_biguint<Width>>::type;
template <int Width> using prism_v2sc_int_t = typename std::conditional<(Width <= 64), sc_int<Width>, sc_bigint<Width>>::type;
constexpr int prism_v2sc_clog2(int value) { int result = 0; for (int current = value - 1; current > 0; current >>= 1) ++result; return result; }
#endif

#ifndef PRISM_V2SC_MOD_E603_GNRL_RAM__PRISM_P_FEBA563485_HPP
#define PRISM_V2SC_MOD_E603_GNRL_RAM__PRISM_P_FEBA563485_HPP

template <int DP = 1024, int DW = 32, int FORCE_X2ZERO = 0, int GATE_CLK = 0, int MW = 4, int AW = 10>
SC_MODULE(e603_gnrl_ram__prism_p_feba563485) {
  sc_in<bool> sd;
  sc_in<bool> ds;
  sc_in<bool> ls;
  sc_in<bool> rst_n;
  sc_in<bool> clk;
  sc_in<bool> clkgate_bypass;
  sc_in<bool> cs;
  sc_in<bool> we;
  sc_in<prism_v2sc_uint_t<(((AW-1) >= (0)) ? ((AW-1) - (0) + 1) : ((0) - (AW-1) + 1))>> addr;
  sc_in<prism_v2sc_uint_t<(((DW-1) >= (0)) ? ((DW-1) - (0) + 1) : ((0) - (DW-1) + 1))>> din;
  sc_in<prism_v2sc_uint_t<(((MW-1) >= (0)) ? ((MW-1) - (0) + 1) : ((0) - (MW-1) + 1))>> wem;
  sc_out<prism_v2sc_uint_t<(((DW-1) >= (0)) ? ((DW-1) - (0) + 1) : ((0) - (DW-1) + 1))>> dout;

  sc_signal<bool> clk_cg;

  e603_gnrl_tech_ram__prism_p_88c924c340<1024, 0, 32, 4, 10> u_ram;

  void assign_0() {
    clk_cg.write(clk.read());
  }

  SC_CTOR(e603_gnrl_ram__prism_p_feba563485)
    : u_ram("u_ram")
  {
    SC_METHOD(assign_0);
    sensitive << clk;

    u_ram.sd(sd);
    u_ram.ds(ds);
    u_ram.ls(ls);
    u_ram.rst_n(rst_n);
    u_ram.clk(clk_cg);
    u_ram.din(din);
    u_ram.addr(addr);
    u_ram.cs(cs);
    u_ram.we(we);
    u_ram.wem(wem);
    u_ram.dout(dout);
  }
};

#endif  // PRISM_V2SC_MOD_E603_GNRL_RAM__PRISM_P_FEBA563485_HPP
