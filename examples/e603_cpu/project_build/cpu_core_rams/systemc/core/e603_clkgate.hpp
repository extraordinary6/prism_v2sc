// Generated by prism_v2sc
#pragma once

#include <array>
#include <systemc>
#include <string>
#include <type_traits>

#include "../tech/e603_gnrl_tech_clkgate.hpp"

using namespace sc_core;
using namespace sc_dt;

#ifndef PRISM_V2SC_INTEGER_TYPES
#define PRISM_V2SC_INTEGER_TYPES
template <int Width> using prism_v2sc_uint_t = typename std::conditional<(Width <= 64), sc_uint<Width>, sc_biguint<Width>>::type;
template <int Width> using prism_v2sc_int_t = typename std::conditional<(Width <= 64), sc_int<Width>, sc_bigint<Width>>::type;
constexpr int prism_v2sc_clog2(int value) { int result = 0; for (int current = value - 1; current > 0; current >>= 1) ++result; return result; }
#endif

#ifndef PRISM_V2SC_MOD_E603_CLKGATE_HPP
#define PRISM_V2SC_MOD_E603_CLKGATE_HPP

SC_MODULE(e603_clkgate) {
  sc_in<bool> clk_in;
  sc_in<bool> clkgate_bypass;
  sc_in<bool> clock_en;
  sc_out<bool> clk_out;

  e603_gnrl_tech_clkgate u_cg;

  SC_CTOR(e603_clkgate)
    : u_cg("u_cg")
  {
    u_cg.clk_in(clk_in);
    u_cg.clkgate_bypass(clkgate_bypass);
    u_cg.clock_en(clock_en);
    u_cg.clk_out(clk_out);
  }
};

#endif  // PRISM_V2SC_MOD_E603_CLKGATE_HPP
